From 93071fe8720b8380acca4da2f89875c6f4bd4622 Mon Sep 17 00:00:00 2001
From: Seas0 <season1277@outlook.com>
Date: Thu, 18 Jun 2026 01:30:50 +0800
Subject: [PATCH] feat: gate sparse gemv single-batch generation

---
 scripts/bench_sparse_kernel_throughput.py |  1 +
 scripts/run_sparse_kernel_verification.sh | 91 ++++++++++++++++++-----
 tests/sparsity/test_distribution.py       | 39 ++++++++++
 vllm/sparsity/distribution.py             | 24 ++++++
 4 files changed, 138 insertions(+), 17 deletions(-)

diff --git a/scripts/bench_sparse_kernel_throughput.py b/scripts/bench_sparse_kernel_throughput.py
index 1f38e59d288..a0470af06df 100644
--- a/scripts/bench_sparse_kernel_throughput.py
+++ b/scripts/bench_sparse_kernel_throughput.py
@@ -425,6 +425,7 @@ def main() -> int:
         "output_len": args.output_len,
         "num_prompts": args.num_prompts,
         "dtype": args.dtype,
+        "sparse_linear_policy": os.environ.get("VLLM_SPARSE_GEMV_LINEAR_POLICY"),
         "require_sparse_gemv_kernel": not args.allow_sparse_gemv_fallback,
         "dense": dense,
         "sparse": sparse,
diff --git a/scripts/run_sparse_kernel_verification.sh b/scripts/run_sparse_kernel_verification.sh
index 2eca7167291..6306916ed47 100755
--- a/scripts/run_sparse_kernel_verification.sh
+++ b/scripts/run_sparse_kernel_verification.sh
@@ -16,23 +16,30 @@ RUN_THROUGHPUT="${RUN_THROUGHPUT:-1}"
 RUN_QWEN_PPL="${RUN_QWEN_PPL:-1}"
 BENCH_WARMUP="${BENCH_WARMUP:-20}"
 BENCH_ITERS="${BENCH_ITERS:-100}"
+BENCH_DTYPE="${BENCH_DTYPE:-float16}"
+MODEL_DTYPE="${MODEL_DTYPE:-float16}"
 MAX_SPARSE_ERR="${MAX_SPARSE_ERR:-0.5}"
 MAX_DIRECT_ERR="${MAX_DIRECT_ERR:-0.5}"
+MAX_DIRECT_T_ERR="${MAX_DIRECT_T_ERR:-0.5}"
+BENCH_SKIP_DIRECT="${BENCH_SKIP_DIRECT:-1}"
 MIN_PACKED_TOTAL_SPEEDUP="${MIN_PACKED_TOTAL_SPEEDUP:-1.0}"
 MIN_PACKED_TOTAL_WITH_THRESHOLD_SPEEDUP="${MIN_PACKED_TOTAL_WITH_THRESHOLD_SPEEDUP:-1.0}"
 MIN_PACKED_COMPUTE_SPEEDUP="${MIN_PACKED_COMPUTE_SPEEDUP:-1.0}"
-THROUGHPUT_NUM_PROMPTS="${THROUGHPUT_NUM_PROMPTS:-32}"
+THROUGHPUT_NUM_PROMPTS="${THROUGHPUT_NUM_PROMPTS:-1}"
 BENCH_BATCH_SIZES="${BENCH_BATCH_SIZES:-1 ${THROUGHPUT_NUM_PROMPTS}}"
-THROUGHPUT_WARMUP_PROMPTS="${THROUGHPUT_WARMUP_PROMPTS:-4}"
+THROUGHPUT_WARMUP_PROMPTS="${THROUGHPUT_WARMUP_PROMPTS:-0}"
 THROUGHPUT_INPUT_LEN="${THROUGHPUT_INPUT_LEN:-128}"
 THROUGHPUT_OUTPUT_LEN="${THROUGHPUT_OUTPUT_LEN:-128}"
 MIN_TOTAL_TOKEN_SPEEDUP="${MIN_TOTAL_TOKEN_SPEEDUP:-1.0}"
 MIN_OUTPUT_TOKEN_SPEEDUP="${MIN_OUTPUT_TOKEN_SPEEDUP:-1.0}"
+THROUGHPUT_SPARSE_LINEAR_POLICY="${THROUGHPUT_SPARSE_LINEAR_POLICY:-auto}"
+PPL_SPARSE_LINEAR_POLICY="${PPL_SPARSE_LINEAR_POLICY:-all}"
 
 export ASCEND_RT_VISIBLE_DEVICES
 export VLLM_TARGET_DEVICE="${VLLM_TARGET_DEVICE:-npu}"
 export VLLM_VERSION="${VLLM_VERSION:-0.17.2}"
 export VLLM_WORKER_MULTIPROC_METHOD="${VLLM_WORKER_MULTIPROC_METHOD:-spawn}"
+export VLLM_ASCEND_FORCE_CPU_SLOT_MAPPING="${VLLM_ASCEND_FORCE_CPU_SLOT_MAPPING:-1}"
 export PYTHONPATH="${VLLM_ROOT}:${ASCEND_ROOT}:${PYTHONPATH:-}"
 
 mkdir -p "${RESULT_DIR}"
@@ -99,8 +106,18 @@ for prefix in ("sparse_gemv", "ascend_sparse_linear"):
             ),
         )
         require(
-            any(int(numel) > 1 for numel in threshold_numels),
-            f"{prefix} La RoSA marker did not prove a batched row threshold",
+            any(int(numel) > 1 for numel in threshold_numels)
+            or any(
+                isinstance(shape, list)
+                and shape
+                and int(shape[0]) == 1
+                for shape in x_shapes
+            ),
+            (
+                f"{prefix} La RoSA marker did not prove batched or "
+                f"single-row top-k threshold, got "
+                f"threshold_numels={threshold_numels}, x_shapes={x_shapes}"
+            ),
         )
         require(
             inclusives == [True],
@@ -111,8 +128,14 @@ for prefix in ("sparse_gemv", "ascend_sparse_linear"):
 
 ops = sparse.get("ascend_sparse_linear_marker_ops", [])
 require(
-    "activation_sparse_linear_packed_t" in ops,
-    f"Ascend packed custom op marker is missing, got {ops}",
+    any(
+        op in ops
+        for op in (
+            "activation_sparse_linear_packed_t",
+            "activation_sparse_linear_direct_t",
+        )
+    ),
+    f"Ascend sparse custom op marker is missing, got {ops}",
 )
 weight_t_values = sparse.get("ascend_sparse_linear_marker_weight_t_provided", [])
 require(
@@ -187,8 +210,18 @@ for prefix in ("sparse_gemv", "ascend_sparse_linear"):
             ),
         )
         require(
-            any(int(numel) > 1 for numel in threshold_numels),
-            f"{prefix} La RoSA marker did not prove a batched row threshold",
+            any(int(numel) > 1 for numel in threshold_numels)
+            or any(
+                isinstance(shape, list)
+                and shape
+                and int(shape[0]) == 1
+                for shape in x_shapes
+            ),
+            (
+                f"{prefix} La RoSA marker did not prove batched or "
+                f"single-row top-k threshold, got "
+                f"threshold_numels={threshold_numels}, x_shapes={x_shapes}"
+            ),
         )
         require(
             inclusives == [True],
@@ -199,8 +232,14 @@ for prefix in ("sparse_gemv", "ascend_sparse_linear"):
 
 ops = markers.get("ascend_sparse_linear_marker_ops", [])
 require(
-    "activation_sparse_linear_packed_t" in ops,
-    f"Ascend packed custom op marker is missing, got {ops}",
+    any(
+        op in ops
+        for op in (
+            "activation_sparse_linear_packed_t",
+            "activation_sparse_linear_direct_t",
+        )
+    ),
+    f"Ascend sparse custom op marker is missing, got {ops}",
 )
 weight_t_values = markers.get("ascend_sparse_linear_marker_weight_t_provided", [])
 require(
@@ -218,6 +257,10 @@ echo "PYTHON_BIN=${PYTHON_BIN}"
 echo "RESULT_DIR=${RESULT_DIR}"
 echo "ASCEND_RT_VISIBLE_DEVICES=${ASCEND_RT_VISIBLE_DEVICES}"
 echo "BENCH_BATCH_SIZES=${BENCH_BATCH_SIZES}"
+echo "MODEL_DTYPE=${MODEL_DTYPE}"
+echo "THROUGHPUT_SPARSE_LINEAR_POLICY=${THROUGHPUT_SPARSE_LINEAR_POLICY}"
+echo "PPL_SPARSE_LINEAR_POLICY=${PPL_SPARSE_LINEAR_POLICY}"
+echo "VLLM_ASCEND_FORCE_CPU_SLOT_MAPPING=${VLLM_ASCEND_FORCE_CPU_SLOT_MAPPING}"
 "${PYTHON_BIN}" - <<'PY'
 import torch
 print("torch", torch.__version__)
@@ -305,7 +348,10 @@ manifest = {
             "csrc/torch_binding.cpp",
             "csrc/torch_binding_meta.cpp",
             "csrc/ops.h",
+            "vllm_ascend/compilation/passes/base_pattern.py",
+            "vllm_ascend/ops/layernorm.py",
             "vllm_ascend/ops/sparse_linear.py",
+            "vllm_ascend/worker/block_table.py",
             "tests/ut/ops/test_sparse_linear.py",
             "benchmarks/ops/bench_activation_sparse_linear.py",
             "benchmarks/ops/run_activation_sparse_linear_matrix.py",
@@ -332,6 +378,8 @@ echo "=== python syntax checks ==="
     scripts/bench_sparse_kernel_throughput.py \
     scripts/eval_teal_ppl_alignment.py \
     scripts/eval_larosa_ppl_alignment.py \
+    tests/sparsity/test_distribution.py \
+    vllm/sparsity/distribution.py \
     vllm/sparsity/kernels/sparse_gemv.py
 )
 (
@@ -382,7 +430,7 @@ def sparse_tolerances(dtype: torch.dtype) -> tuple[float, float]:
 require(hasattr(torch, "npu") and torch.npu.is_available(), "NPU is required")
 require(_custom_op_enabled(), "Ascend custom ops must be enabled")
 torch.manual_seed(0)
-for dtype in (torch.float16, torch.bfloat16):
+for dtype in (torch.float16,):
     x = torch.randn(3, 32, device="npu", dtype=dtype)
     weight = torch.randn(17, 32, device="npu", dtype=dtype)
     threshold = torch.tensor([0.25, 0.5, 0.75], device="npu")
@@ -456,15 +504,20 @@ BENCH_BATCH_ARGS=()
 for batch_size in "${BENCH_BATCH_SIZE_ARRAY[@]}"; do
   BENCH_BATCH_ARGS+=(--default-batch-size "${batch_size}")
 done
+BENCH_DIRECT_ARGS=(--skip-direct)
+if [[ "${BENCH_SKIP_DIRECT}" != "1" ]]; then
+  BENCH_DIRECT_ARGS=(--max-direct-err "${MAX_DIRECT_ERR}")
+fi
 (
   cd "${ASCEND_ROOT}"
   "${PYTHON_BIN}" benchmarks/ops/run_activation_sparse_linear_matrix.py \
-    --dtype bfloat16 \
+    --dtype "${BENCH_DTYPE}" \
     "${BENCH_BATCH_ARGS[@]}" \
     --warmup "${BENCH_WARMUP}" \
     --iters "${BENCH_ITERS}" \
     --max-sparse-err "${MAX_SPARSE_ERR}" \
-    --max-direct-err "${MAX_DIRECT_ERR}" \
+    --max-direct-t-err "${MAX_DIRECT_T_ERR}" \
+    "${BENCH_DIRECT_ARGS[@]}" \
     --min-packed-total-speedup "${MIN_PACKED_TOTAL_SPEEDUP}" \
     --min-packed-total-with-threshold-speedup "${MIN_PACKED_TOTAL_WITH_THRESHOLD_SPEEDUP}" \
     --min-packed-compute-speedup "${MIN_PACKED_COMPUTE_SPEEDUP}" \
@@ -475,6 +528,7 @@ if [[ "${RUN_THROUGHPUT}" == "1" ]]; then
   echo "=== Qwen2.5 TEAL kernel-required throughput ==="
   (
     cd "${VLLM_ROOT}"
+    VLLM_SPARSE_GEMV_LINEAR_POLICY="${THROUGHPUT_SPARSE_LINEAR_POLICY}" \
     "${PYTHON_BIN}" scripts/bench_sparse_kernel_throughput.py \
       --model "${MODEL}" \
       --method teal \
@@ -484,7 +538,7 @@ if [[ "${RUN_THROUGHPUT}" == "1" ]]; then
       --warmup-prompts "${THROUGHPUT_WARMUP_PROMPTS}" \
       --input-len "${THROUGHPUT_INPUT_LEN}" \
       --output-len "${THROUGHPUT_OUTPUT_LEN}" \
-      --dtype bfloat16 \
+      --dtype "${MODEL_DTYPE}" \
       --gpu-memory-utilization "${VLLM_GPU_MEMORY_UTILIZATION:-0.75}" \
       --vllm-prefill-sparsify none \
       --min-total-token-speedup "${MIN_TOTAL_TOKEN_SPEEDUP}" \
@@ -498,6 +552,7 @@ if [[ "${RUN_THROUGHPUT}" == "1" ]]; then
   echo "=== Qwen2.5 La RoSA kernel-required throughput ==="
   (
     cd "${VLLM_ROOT}"
+    VLLM_SPARSE_GEMV_LINEAR_POLICY="${THROUGHPUT_SPARSE_LINEAR_POLICY}" \
     "${PYTHON_BIN}" scripts/bench_sparse_kernel_throughput.py \
       --model "${MODEL}" \
       --method larosa \
@@ -507,7 +562,7 @@ if [[ "${RUN_THROUGHPUT}" == "1" ]]; then
       --warmup-prompts "${THROUGHPUT_WARMUP_PROMPTS}" \
       --input-len "${THROUGHPUT_INPUT_LEN}" \
       --output-len "${THROUGHPUT_OUTPUT_LEN}" \
-      --dtype bfloat16 \
+      --dtype "${MODEL_DTYPE}" \
       --gpu-memory-utilization "${VLLM_GPU_MEMORY_UTILIZATION:-0.75}" \
       --min-total-token-speedup "${MIN_TOTAL_TOKEN_SPEEDUP}" \
       --min-output-token-speedup "${MIN_OUTPUT_TOKEN_SPEEDUP}" \
@@ -522,6 +577,7 @@ if [[ "${RUN_QWEN_PPL}" == "1" ]]; then
   echo "=== Qwen2.5 TEAL kernel-required PPL ==="
   (
     cd "${VLLM_ROOT}"
+    VLLM_SPARSE_GEMV_LINEAR_POLICY="${PPL_SPARSE_LINEAR_POLICY}" \
     "${PYTHON_BIN}" scripts/eval_teal_ppl_alignment.py \
       --model "${MODEL}" \
       --artifact-root "${TEAL_ARTIFACT_ROOT}" \
@@ -540,7 +596,7 @@ if [[ "${RUN_QWEN_PPL}" == "1" ]]; then
       --hf-reference-impl hooks \
       --hf-reference-mode decode_only \
       --vllm-prefill-sparsify none \
-      --dtype bfloat16 \
+      --dtype "${MODEL_DTYPE}" \
       --device npu \
       --vllm-gpu-memory-utilization "${VLLM_GPU_MEMORY_UTILIZATION:-0.75}" \
       --vllm-use-sparse-gemv \
@@ -554,6 +610,7 @@ if [[ "${RUN_QWEN_PPL}" == "1" ]]; then
   echo "=== Qwen2.5 La RoSA kernel-required PPL ==="
   (
     cd "${VLLM_ROOT}"
+    VLLM_SPARSE_GEMV_LINEAR_POLICY="${PPL_SPARSE_LINEAR_POLICY}" \
     "${PYTHON_BIN}" scripts/eval_larosa_ppl_alignment.py \
       --model "${MODEL}" \
       --calibration-path "${LAROSA_ARTIFACT_ROOT}" \
@@ -570,7 +627,7 @@ if [[ "${RUN_QWEN_PPL}" == "1" ]]; then
       --max-windows "${PPL_MAX_WINDOWS:-32}" \
       --backend both \
       --hf-reference-impl hooks \
-      --dtype bfloat16 \
+      --dtype "${MODEL_DTYPE}" \
       --device npu \
       --vllm-gpu-memory-utilization "${VLLM_GPU_MEMORY_UTILIZATION:-0.75}" \
       --vllm-use-sparse-gemv \
diff --git a/tests/sparsity/test_distribution.py b/tests/sparsity/test_distribution.py
index 20b88812303..cbd9eed8648 100644
--- a/tests/sparsity/test_distribution.py
+++ b/tests/sparsity/test_distribution.py
@@ -289,6 +289,45 @@ def test_sparse_linear_weight_t_cache_invalidates_after_weight_mutation():
     assert not torch.allclose(first, linear.weight.t())
 
 
+def test_sparse_linear_auto_policy_keeps_single_batch_wide_output(monkeypatch):
+    monkeypatch.delenv("VLLM_SPARSE_GEMV_LINEAR_POLICY", raising=False)
+    sparsify = SparsifyFn(torch.tensor(0.5), use_sparse_gemv=True)
+    x = torch.randn(1, 3584)
+    weight = torch.randn(32768, 3584)
+
+    assert sparsify._should_use_sparse_linear_kernel(x, weight) is True
+
+
+def test_sparse_linear_auto_policy_skips_losing_shapes(monkeypatch):
+    monkeypatch.delenv("VLLM_SPARSE_GEMV_LINEAR_POLICY", raising=False)
+    sparsify = SparsifyFn(torch.tensor(0.5), use_sparse_gemv=True)
+
+    assert not sparsify._should_use_sparse_linear_kernel(
+        torch.randn(2, 3584),
+        torch.randn(32768, 3584),
+    )
+    assert not sparsify._should_use_sparse_linear_kernel(
+        torch.randn(1, 3584),
+        torch.randn(4608, 3584),
+    )
+    assert not sparsify._should_use_sparse_linear_kernel(
+        torch.randn(1, 18944),
+        torch.randn(3584, 18944),
+    )
+
+
+def test_sparse_linear_policy_overrides(monkeypatch):
+    sparsify = SparsifyFn(torch.tensor(0.5), use_sparse_gemv=True)
+    x = torch.randn(2, 4)
+    weight = torch.randn(3, 4)
+
+    monkeypatch.setenv("VLLM_SPARSE_GEMV_LINEAR_POLICY", "all")
+    assert sparsify._should_use_sparse_linear_kernel(x, weight) is True
+
+    monkeypatch.setenv("VLLM_SPARSE_GEMV_LINEAR_POLICY", "none")
+    assert sparsify._should_use_sparse_linear_kernel(x, weight) is False
+
+
 def test_sparse_gemv_marker_records_tensor_metadata(tmp_path, monkeypatch):
     from vllm.sparsity.kernels.sparse_gemv import (
         _record_sparse_gemv_invocation,
diff --git a/vllm/sparsity/distribution.py b/vllm/sparsity/distribution.py
index 72f981cb38b..af2c59d559f 100644
--- a/vllm/sparsity/distribution.py
+++ b/vllm/sparsity/distribution.py
@@ -1,6 +1,8 @@
 # SPDX-License-Identifier: Apache-2.0
 """TEAL and La RoSA distribution/sparsify functions."""
 
+import os
+
 import torch
 from torch import nn
 
@@ -161,6 +163,8 @@ class SparsifyFn(nn.Module):
         sparse_input = self._sparse_linear_input(x, linear_layer)
         if sparse_input is None:
             return None
+        if not self._should_use_sparse_linear_kernel(sparse_input, weight):
+            return None
 
         threshold, inclusive = self._sparse_linear_threshold(sparse_input)
         if threshold is None:
@@ -202,6 +206,26 @@ class SparsifyFn(nn.Module):
         linear_layer._activation_sparse_weight_t_key = cache_key
         return weight_t
 
+    def _should_use_sparse_linear_kernel(
+        self,
+        sparse_input: torch.Tensor,
+        weight: torch.Tensor,
+    ) -> bool:
+        mode = os.environ.get("VLLM_SPARSE_GEMV_LINEAR_POLICY", "auto").lower()
+        if mode in {"all", "always"}:
+            return True
+        if mode in {"none", "never"}:
+            return False
+        if mode != "auto":
+            return False
+        return (
+            sparse_input.dim() == 2
+            and sparse_input.shape[0] == 1
+            and sparse_input.shape[1] <= 4096
+            and weight.dim() == 2
+            and weight.shape[0] >= 32768
+        )
+
     def _sparse_linear_applies_to_all_rows(self, x: torch.Tensor) -> bool:
         if self.apply_all_tokens or self.prefill_sparsify == "all":
             return True
-- 
2.53.0.windows.3

